/**
 * SQ.AudioGenerator — TTS narration generation (Phase 3).
 * Placeholder — will use OpenRouter audio modality.
 */
(function () {
  SQ.AudioGenerator = {
    // TODO: Phase 3 — Implement TTS narration
    // Uses OpenRouter with modalities: ["text", "audio"]
    // Stream audio and play via Web Audio API / HTML5 audio
    generate: function (passageText) {
      console.log('AudioGenerator: Phase 3 — not yet implemented');
      return Promise.resolve(null);
    }
  };
})();
