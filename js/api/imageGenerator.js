/**
 * SQ.ImageGenerator — Illustration generation (Phase 2).
 * Placeholder — will use OpenRouter image modality.
 */
(function () {
  SQ.ImageGenerator = {
    // TODO: Phase 2 — Implement illustration generation
    // Uses OpenRouter with modalities: ["image"]
    // Visual style prefix from SQ.PlayerConfig
    generate: function (illustrationPrompt) {
      console.log('ImageGenerator: Phase 2 — not yet implemented');
      return Promise.resolve(null);
    }
  };
})();
